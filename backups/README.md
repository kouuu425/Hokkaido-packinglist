# backups

`index.html` からの退避データを置く場所。

- `packing-list-2026-08-08.json` — 「持ち物」セクションを一旦削除する際にバックアップした持ち物リスト（カテゴリ・項目の定義）。復元する際は `index.html` にあった `DEFAULTS` 配列と、持ち物セクションの HTML/CSS/JS（Gitの履歴から復元可能）と合わせて使う。
